// Placeholder: ensures `tsc -p tsconfig.web.json` has at least one input until
// Task 2 (Electron shell) and Task 3 (shared wire types) populate src/shared/
// and src/renderer/src/. Safe to delete once the real files exist.
export {};