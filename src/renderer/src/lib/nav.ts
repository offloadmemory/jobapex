/**
 * Sidebar pages. Declared outside App.tsx so the composer's slash commands can
 * name a target without importing the shell.
 */
export type NavId = "chat" | "threads" | "skills" | "memory" | "files" | "settings";
