import type { ChatStore } from "../lib/chat-store";

/**
 * Stub — the real approval dialog lands in Task 9 (resolveApproval wiring).
 * Rendered as a fixed slot in ChatView so layout stays stable.
 */
export function ApprovalCard(_props: { store: ChatStore }) {
  return null;
}